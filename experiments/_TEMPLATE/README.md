# Experiment NNNN — <name>

## Hypothesis (write BEFORE running)
> H:
> Decision rule:

## Pre-registered prediction
<What do you actually expect to happen, and how confident? Write a number.
You commit this before the run. Being wrong here is the most useful outcome.>

## Setup
- **Baseline:** <known number you reproduced + where it's from>
- **The one variable changed:**
- **Held fixed:** <everything else — data, seed, steps, model size>
- **Seed:**
- **Config:** see `config.yaml`
- **Git commit at run time:** `<filled by run>`
- **Environment:** `<paste `uv pip freeze` hash or key versions>`

## Run
```bash
# exact command(s) to reproduce this experiment
```

## Results (raw)
<Tables/numbers as they came out. No editing to look nice. Link plots/logs.>

## Conclusion
- Outcome: supported / rejected / inconclusive
- Effect size:
- Why (especially if rejected — what's your explanation):
- Next variable to test / what this rules out:
